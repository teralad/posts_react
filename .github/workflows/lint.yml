name: Lint

on: pull_request

jobs:
  eslint:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v1
        with:
          fetch-depth: 1
      - uses: actions/setup-node@v1
        with:
          node-version: 12
      - uses: teralad/posts_react@v1
        with:
          repo-token: 600b193032e2dd0d18a597528771e6a520f1bb34
          check-name: eslint # this is the check name from above 👆 where to post annotations
